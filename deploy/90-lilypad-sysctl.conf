# Lily Pad hardening: disable the magic SysRq key entirely (belt-and-braces;
# the exclusive evdev grab already keeps keystrokes away from the kernel
# console handler — RESEARCH.md §2/§4).
kernel.sysrq = 0
